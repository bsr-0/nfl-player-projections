# Minimal dependencies for running the web API server only.
# Use this for production deployment when models/data are pre-computed.
# For full ML training pipeline, use requirements.txt instead.

# Core data
pandas==2.1.4
numpy==1.26.3
requests==2.31.0
certifi>=2023.11.17

# API server
fastapi==0.109.0
uvicorn==0.27.0
gunicorn==21.2.0

# Data storage
sqlalchemy==2.0.25

# Utilities
python-dateutil==2.8.2
joblib==1.3.2

# Visualization (used by app_data.py)
plotly>=5.18.0

# ML inference only (no training)
scikit-learn==1.4.0
xgboost==2.0.3
lightgbm==4.3.0

# NFL data source (schedules/rosters refresh in generate_app_data)
nfl-data-py>=0.3.0
